# Reading

1. What does the `<canvas>` allow a developer to acheive?

    It allows you to draw 2D graphics using JavaScript.

2. What is the importance of the closing `</canvas> tag?

    The canvas closing tag marks the end of the canvas which the browser may not understand where the canvas stops which may or may not correctly render you're page.

3. Explain what the `getContext()` method does.

    It's like picking a tool to draw on the canvas, it tells the browser what kind of drawing you want to do.

1. What is Chart.js and how it can be brought into your project?

    Chart.js is a library for JavaScript application developers. it could be brought into my project to show a graph of what games I've played the most.

2. List 3 different Chart types you can create using Chart.js.

    Pie graph.
    Bar graph.
    Line graph.

1. What are some advantages to displaying data via a chart over a table?

    They're easier to look at, the data is easier to remember from a chart.

2. How could Chart.js aid your previously created applications visually?

    It could make my mom website more visually appealing, or for salmon cookies it could replace the table.