# HTML Forms

1. Why are forms so important in web development?

Forms are how users create accounts, log in, search, submit data, configure settings and/or make purchases.

2. When designing a form, what are some key things to keep in mind when it comes to user experience?

The bigger the form the worse you're UX will be, it's important to reduce user effort. labeling and adding instructions are also important.

3. List 5 form elements and explain their importance.

<input> collects user data in various formats (text, email, password), making it essential for user interaction.

<label> describes and links to a form control, improving usability and accessibility.

<textarea> allows users to enter multi-line text, which is important for longer responses.

<select> lets users choose from predefined options, reducing input errors.

<button> triggers form actions such as submission, enabling the form to function.

## Learn JS

1. How would you describe events to a non-technical friend?

It's like being on edge, you're waiting for something-anything to change.

2. When using the `addEventListener()` method, what 2 arguments will you need to provide?

A callback function is a function that is passed into another function as an argument, with the expectation that it will be called later.

3. Describe the event object. Why is the target within the event object useful?

The event object contains information about an event that occurred, and its target property is useful because it identifies the exact element that triggered the event, allowing precise event handling.

4. What is the difference between event bubbling and event capturing?

Event capturing handles an event from the outermost parent down to the target element, while event bubbling handles the event from the target element back up through its parent elements.
